("""Model package for Pydantic schemas used across the backend.

This package contains request/response models and domain schemas (e.g.
`Trip`, `User`, `FileMetadata`) used by API endpoints and internal
services. Individual model modules are imported by name where needed.
""")

