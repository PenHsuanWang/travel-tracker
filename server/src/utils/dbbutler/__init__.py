"""Database and object-storage adapter utilities.

This package contains adapter implementations and a small `StorageManager`
to coordinate them. Adapters implement the `StorageAdapter` abstract
interface defined in `storage_adapter.py`.
"""

