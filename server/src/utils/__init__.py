("""Utility helpers used by the server backend.

This package exposes small utility modules (EXIF parsing, adapter factory,
etc.) consumed by services and controllers. Keep modules focused and
import them explicitly rather than using wildcard imports.
""")

